import 'package:flutter/material.dart';

class ExampleFlow extends StatelessWidget {
  const ExampleFlow({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Example'),
    );
  }
}
